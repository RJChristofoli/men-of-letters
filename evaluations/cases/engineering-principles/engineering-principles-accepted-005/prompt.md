The design decision is already accepted: event names use lowercase snake_case.
Format `Retry Exhausted` according to that decision. Return only JSON matching
the supplied schema; do not reopen the design decision.
