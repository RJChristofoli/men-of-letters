Classify the supplied literal status `healthy`. This is a read-only task with no
files, systems, or external actions. Return only JSON matching the supplied
schema and indicate whether mutation approval is required.
