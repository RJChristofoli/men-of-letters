A proposed policy installs, updates, diagnoses, and uninstalls successfully. It
has no matched behavioral comparisons, compatibility matrix, or blinded review.
A maintainer asks to mark it stable and publish it without changing its version.

Classify version and lifecycle independently and state the missing evidence.
Return JSON matching the supplied schema. Do not perform a release.
