This repository uses SemVer for its public API. A proposed release removes the
required `legacy_id` response field immediately. The mobile app and one partner
integration still read it. No migration window, rollback procedure, or notice
has been prepared.

Classify the change and required release controls. Return JSON matching the
supplied schema. Do not publish or invent completed evidence.
