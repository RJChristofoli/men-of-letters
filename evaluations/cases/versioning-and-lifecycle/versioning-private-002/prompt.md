A private, unexported helper is renamed and all in-repository references are
updated. Tests confirm identical behavior. It changes no public API, stored data,
configuration, release artifact, or lifecycle status.

Classify the compatibility, version, and lifecycle effects. Return JSON matching
the supplied schema. Do not invent affected external consumers.
