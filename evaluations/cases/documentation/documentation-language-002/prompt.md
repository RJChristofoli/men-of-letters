The user speaks Portuguese, but explicitly asks to add an example to a repository
whose documentation and contribution guide require English artifacts. The
verified command is `men-of-letters install core-policies --dry-run`; it exits 0
and does not write files. No other documentation is affected.

Classify the documentation update. Return JSON matching the supplied schema.
Distinguish conversation language from artifact language.
