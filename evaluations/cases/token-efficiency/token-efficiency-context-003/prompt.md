Inspect `release.json`, `verification.json`, and `rollback.md`, then produce a
release handoff for the current API candidate. `historical-notes.md` is obsolete:
identify it as ignored context without reading or summarizing its contents.
Return JSON matching the supplied schema. Use no external information and batch
the required local reads when possible.
